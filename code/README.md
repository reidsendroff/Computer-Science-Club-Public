# Code Index

All Java source should compile with the school-supported JDK version (`TBD`). Do not commit `.class` files.

## Examples

- [Club fair demo](club-fair/ClubFairDemo.java) — change a choice and see a different activity message

Local commands from each file's directory:

```sh
javac FileName.java
java FileName
```

The verified zero-install browser path remains `TBD` pending school-device and privacy checks.
